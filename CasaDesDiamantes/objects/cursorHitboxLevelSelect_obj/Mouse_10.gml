if (instance_exists(overworld_obj)) {
	if (overworld_obj.menu == 0) {
		if (index < 7) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 1) {
		if (index < 3) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 2) {
		if (index < 7) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 3) {
		if (index < 5) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 4) {
		if (index < 3) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 5) {
		if (index < 5) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 6) {
		if (index < 5) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
	if (overworld_obj.menu == 7) {
		if (index < 5) {
			open = true;
			audio_play_sound(typewriter_snd, 1, false);
		}
	}
}